<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="it.mem.guessthenumber">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GuessANumber_2"
        tools:ignore="AllowBackup">
        <activity android:name="it.mem.guessthenumber.Game">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="it.mem.guessthenumber.SplashScreen"
            android:parentActivityName="it.mem.guessthenumber.Level" />
        <activity
            android:name="it.mem.guessthenumber.Level"
            android:parentActivityName="it.mem.guessthenumber.SecondActivity" />
        <activity android:name="it.mem.guessthenumber.SecondActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>