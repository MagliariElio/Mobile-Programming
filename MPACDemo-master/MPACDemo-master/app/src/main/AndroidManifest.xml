<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it.massimoregoli.mpacdemo">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".activities.chart.CandleStickChartActivity"></activity>
        <activity android:name=".activities.chart.BubbleChartActivity" />
        <activity android:name=".activities.chart.PieChartActivity" />
        <activity android:name=".activities.chart.BarChartActivity" />
        <activity android:name=".activities.InsertDataActivity" />
        <activity android:name=".SelectionActivity" />
        <activity android:name=".activities.chart.LineChartActivity" />
        <activity android:name=".activities.chart.ScatterChartActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>